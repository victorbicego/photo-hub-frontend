import { HostPhotoUrlPipe } from './host-photo-url.pipe';

describe('HostPhotoUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new HostPhotoUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
